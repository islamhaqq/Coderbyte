const assert = require('assert');

const LongestWord = require('./');

// Unit test LongestWord.
assert.equal(LongestWord("I'll be back!"), 'back', 'LongestWord should get the longest word from a sentence');
